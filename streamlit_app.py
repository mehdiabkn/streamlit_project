import streamlit as st
import plotly.express as px
from datetime import datetime

# Set up the title and date selector
st.title("Sunburst of tatata")
#selected_date = st.date_input("Select a date", datetime.today())

# Create the sunburst chart
#fig = px.sunburst(
#    names=["tatata", "foo", "bar", "baz", "qux", "quux"],
#    parents=["", "tatata", "tatata", "foo", "foo", "bar"],
#    values=[10, 5, 3, 2, 2, 1],
#)
#st.plotly_chart(fig)
